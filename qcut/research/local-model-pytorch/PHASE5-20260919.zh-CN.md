# 第五阶段收尾：平台、编辑器入口与模型精度

日期：2026-09-19。分支 `codex/local-neural-model-audit-20260919`，延续 [PR #477](https://github.com/Quriosity-agent/qcut/pull/477)。用户要求立即收尾后停止扩展；所有实验进程已结束。按单文件、单提交发布，不上传私有模型、字表、张量或媒体。

## 完成范围

| 工作 | 已验证 | 不能据此宣称 |
| --- | --- | --- |
| ONNX 平台 | Windows x64、Linux x64/ARM64、macOS ARM64 公开合成 CI 均通过；Linux x64 私有 19 图、76 组回放通过 | Windows 私有模型已通过；AMD64 容器等于独立 Intel 机器；macOS Intel 可用 |
| 视频分镜 | CLI 新增 `--engine onnx`，仅依赖 Python/NumPy/ORT/FFmpeg；六组实际视频切点与旧 Torch 全部一致；无 Torch 环境 60 秒循环素材得到 39 个切点，耗时 11.685 秒 | 全部概率逐位一致；60 秒素材全部为独立内容；实时性能承诺 |
| 编辑器 | IPC/HTTP 引擎路由、右键 ONNX 入口、过期结果保护、保留引擎重试；FFmpeg 仍默认，无静默回退 | 编辑器完整 E2E 通过；取消 UI 能终止后台推理 |
| OCR | 精确 10,537 类映射与 CTC；11 张中英文条图 native/PT/ONNX 完整 logits 过门槛且文字相同 | 全部文字正确：仅 4/11 完全正确，CER 5.0209%；检测/裁剪/方向/编辑器全链路未验收 |
| GRU 人像抠像 | 新 v3 的 250 次推理、1,000 组完整输出过严格门槛；概率输出全部逐位一致；统一研究 infer 以包 SHA 固定注册 | 全状态逐位一致；跨平台数值 profile、ONNX 或编辑器抠像已接好 |
| Bandou | 新有序 profile 原生 30/30、独立回放 30/30；旧三模型回归 54/54；本机 ONNX 烟测 4/4 | 已注册共享 infer；完整 30 组 ONNX 或目标系统验收；产品祛斑质量已确认 |

旧 GRU v1/v2 和旧失败 Bandou 包保持拒绝；新序列化 profile 不重复计为新模型家族。GRU 的 ONNX 导出明确拒绝未验收的数值适配器。Bandou 新包使用独立严格加载器，尚未加入共享入口。

## 最终回归

- Python 自动发现回归：**604/604**，5.032 秒。
- Electron/CLI 定向 Vitest：**109/109**，7 个测试文件。
- 前端右键菜单/场景路由：**39/39**，2 个测试文件。
- Electron 与前端 `tsc --noEmit` 均通过。已同步 `platform-core` 的引擎、route、duration 类型。
- 子任务 Electron/Vite 构建通过；实际 Electron E2E **2 failed**，导入后报 `Media not found`，未进入推理。因此 split、undo、save/reload、preview/export 的真实链条仍未验收，失败截图保留。
- 新进程封装采用独立 POSIX 进程组，终止/超时同时覆盖 FFmpeg；Windows 使用 `taskkill /T /F`。现有子进程单测通过，但 Windows 编辑器取消及强制升级终止未做媒体 E2E。
- 返回值检查固定模型身份、完整分数、采样参数及阈值；补齐编译目录之外启动时的研究脚本定位候选。
- `git diff --check` 通过。公开平台 CI [35427584529](https://github.com/Quriosity-agent/qcut/actions/runs/35427584529) 四项通过，对应提交 `6786b918f`；不把它冒充最终推送的新 CI。

最终日志：`.local/jianying-model-pytorch/phase5-20260919/{unittest-final,vitest-final,web-tests-final,electron-types-final,web-types-final}.log`。
真实 UI 失败截图与日志：`.local/jianying-model-pytorch/phase5-editor-e2e/`。CLI 视频证据：`.local/jianying-model-pytorch/phase5-shot/`。全部仅保留本机。

## 交接与下一步

1. 先定位编辑器导入后的 `Media not found`，再重跑两项真实 Electron 测试；确认切分、撤销、保存重开与导出。不能仅通过 mock 单测验收。
2. 编辑器异步 job 补后台取消 IPC，再验证取消不留 Python/FFmpeg，不能只忽略过期结果。
3. Windows 受控环境运行私有 19 图与实际分镜视频；不把私有权重上传公开 CI。macOS Intel 需另选并验收可安装的 ORT 构建。
4. Bandou 补完整 ONNX 回放、广播常量压缩独立单测和共享注册。GRU 先验收可移植数值实现，再决定 ONNX 或产品接入。
5. OCR 继续检测、裁剪、方向和输入色彩链；保持真实 CER，不用标点归一化掩盖错误。

详细记录：[分镜](shot-onnx-phase5.zh-CN.md)、[平台](onnx-platform-phase5.zh-CN.md)、[OCR](ocr-alphabet-phase5.zh-CN.md)、[GRU](matting_route.zh-CN.md)、[Bandou](bandou-phase5.zh-CN.md)。第四阶段统计及历史失败保留，不回写成已完成。
