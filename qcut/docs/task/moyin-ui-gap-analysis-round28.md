# Moyin UI Gap Analysis — Round 28: Final Icon Button Labels

## Overview
Round 28 adds `aria-label` to the last remaining icon-only buttons:
edit buttons in property-panel and scene-episode-detail, and the
reference image delete button in property-panel.

## Subtasks

### 28.1 Character Edit Button `aria-label`
Add `aria-label="Edit character"` to the PencilIcon button in
property-panel CharacterDetail.

**Files**:
- `apps/web/src/components/editor/media-panel/views/moyin/property-panel.tsx`

---

### 28.2 Reference Image Delete Button `aria-label`
Add `aria-label="Remove reference image"` to the XIcon delete button
for reference images in property-panel.

**Files**:
- `apps/web/src/components/editor/media-panel/views/moyin/property-panel.tsx`

---

### 28.3 Scene Edit Button `aria-label`
Add `aria-label="Edit scene"` to the PencilIcon button in
scene-episode-detail SceneDetail.

**Files**:
- `apps/web/src/components/editor/media-panel/views/moyin/scene-episode-detail.tsx`
